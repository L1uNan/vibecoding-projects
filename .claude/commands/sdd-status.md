---
description: Show SDD workflow status
---

Read `openspec/.active` and summarize current phase/progress/next step based on:
- `openspec/changes/<name>/task_plan.md`
- which artifacts exist
- tasks completion in `tasks.md` (if present)

If no active change, instruct the user to run `/sdd-new <name>`.
